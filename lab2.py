# Python3 integers have no fix siz.they are automatically
# given more memory by Python, meaning integers have essentially no size limit
# Python returns a float whenever performing operations on a mix of int and float types.
# / --> normal division. If we want to force division to return an int, we can use the integer
# division operator (//).
# 4.0 ==4 (gives true)
#expressions are evaluated before being pass to the print function